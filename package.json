{
  "name": "cline-personas",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "cline-personas": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo 'showing only failing test and coverage data:' && jest",
    "build": "tsc",
    "watch": "tsc -w"
  },
  "author": "Brad Fair",
  "license": "MIT",
  "description": "An MCP Server that lets you implement personas in Cline via the `.clinerules` file.",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.5",
    "jest": "^29.7.0",
    "jest-silent-reporter": "^0.6.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
